// MainClass.java
package labOne;

public class MainClass {
    public static void main(String[] args) {
        // Створення екземплярів класів
        One objOne = new One(10, 3.14, "Hello");
        Two objTwo = new Two();  // Залежно від потреби можна передати параметри конструктору
        Three objThree = new Three();

        // Виклик методів для відповідних екземплярів класів
        objOne.voidMethod();
        int result = objOne.typedMethod(5);
        System.out.println("Result of typedMethod: " + result);

        // Повторіть аналогічні виклики для інших класів
    }
}
