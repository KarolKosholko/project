package labOne;

public class One {
    // інші члени класу залишаються незмінними

    // Перевизначений метод
    public void overriddenMethod(double parameter) {
        // Загальна логіка методу залишається
        System.out.println("Overridden Method in class One with double parameter: " + parameter);
    }

    // Статичний метод
    public static void staticMethod(int value) {
        // Динамічна логіка
        System.out.println("Static Method in class One with int parameter: " + value);
    }
}
